All package requirements are listed in requirements.txt file.
Application is currently dockerized. In order to run it without docker, database host must be changed in movapp/settings.py.
To run the dockerized version, use command: "docker-compose up".